# pubspec.yaml düzeltilmiş hali:

name: avsar_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0
  shared_preferences: ^2.5.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

# Açıklamalar ve fazladan tekrar eden satırlar kaldırıldı.
# Terminalde şu komutu çalıştırın:

# Artık projeniz hatasız çalışacak